package utils;

import com.badlogic.gdx.graphics.g2d.SpriteBatch;
import controllers.GamescreenController;
import model.Ball;
import model.World;
import view.Gamescreen;

public class WorldRenderer {
    SpriteBatch batch;

    public WorldRenderer(SpriteBatch spriteBatch) {
        this.batch = spriteBatch;
    }

    public void render(GamescreenController gamescreenController){
        batch.begin();
        //draw Level
        batch.draw(Assets.player, gamescreenController.getWorld().getPlayer().position.x, gamescreenController.getWorld().getPlayer().position.y);
        if(gamescreenController.getWorld().getPlayer().getShot() != null){
            //draw Shot
        }
        for (Ball ball : gamescreenController.getWorld().getBalls()) {
            //draw Ball
        }
        switch (gamescreenController.getGameState()) {
            case paused:
                //draw paused picture
                break;
            case win:
                //draw win picture
                break;
            case gameover:
                //draw gameover picture
                break;
            case ready:
                break;
        }
        batch.end();
    }
}