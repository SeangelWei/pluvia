package model;

import utils.Assets;
import utils.GameObject;
import utils.Pluvia;

public class Shot extends GameObject {
    int speed;

    public Shot(float x, float y) {
        super(x, y-380);
        speed = 300;
        bounds.setWidth(Assets.shot.getWidth());
        bounds.setHeight(100);
    }

    @Override
    public void update() {
        position.y += Pluvia.delta*speed;
        bounds.height += Pluvia.delta*speed;
    }
}
